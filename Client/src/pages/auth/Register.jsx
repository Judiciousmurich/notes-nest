
const Register = ({ onRegister, }) => {
    return (
        <div>

            <>
                <button onClick={onRegister}>Register</button>
            </>
        </div>
    );
};

export default Register;
